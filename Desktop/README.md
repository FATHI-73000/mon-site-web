## 🔐 Désindexation d’un fichier sensible

Le fichier `.password-prod` a été ajouté par erreur au dépôt. Pour le retirer sans le supprimer localement :

```bash
git rm --cached .password-prod

